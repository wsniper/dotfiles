# by weaming

# some more ls aliases
alias ll='ls -alFh'
alias la='ls -A'
alias l='ls -CF'

# git
alias gitup="git add -A; git commit -m"

# directory
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias home="cd ~"
alias dload="cd ~/下载"
alias github="cd ~/github"
alias conf="cd /root/conf"

# other
alias ssh="ssh -p 22"
alias vi="vim"
alias v="vim"
alias rm="rm -i"
alias df="df -h"
alias blogupdate="cd /www/"
alias f5="source /etc/fish/config.fish"


# files
alias vimrc="vim ~/.vimrc"
alias fishrc="vim /etc/fish/config.fish"

# shell
alias iss="~/shell/ss.sh"
alias isync="~/shell/isync.sh"
alias iwt="~/shell/weather.py"